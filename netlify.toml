# netlify.toml

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# --- ADICIONE ESTE BLOCO ABAIXO ---
[dev]
  # Esta é a porta em que o seu servidor Vite está rodando.
  targetPort = 8080